# Sea Decor Scripts

Runtime helpers for non-interactive ocean scenery.

- `sea_decor_spawner.gd`: keeps a small set of visual-only decor props around the player without blocking ships or reward sites.
