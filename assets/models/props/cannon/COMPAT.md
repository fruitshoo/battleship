# Compatibility Files

These files are kept only because the current imported model still expects them:

- `cannon_cannon_albedo.png`

Preferred managed file:

- `cannon_albedo.png`

Do not delete compatibility files until the source model is re-exported.
